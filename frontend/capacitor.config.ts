import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'adaptive-learning-platform',
  webDir: 'dist'
};

export default config;
